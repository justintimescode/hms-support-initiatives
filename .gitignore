# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# electron-builder output (installers / packaged app)
release/

# Local secrets — Jira API credentials for the dev proxy
.env
.env.*.local

# Local secrets — Jira credentials entered in Settings (dev server only; the
# packaged desktop app stores them encrypted under the user's profile instead)
.jira-creds.json

# Persistent Jira issue cache — large, machine-local
.jira-cache/

# Persistent ServiceNow imports disk mirror — contains customer case data,
# machine-local, large. Must never be committed.
.servicenow-cache/

# Perf harness output (scripts/perf-measure.mjs) — run JSON + browser profile
.perf/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
_sample_validation.xlsx
*.xlsx
